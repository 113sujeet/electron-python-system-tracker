Installe Node JS Firstly

Electron JS requirements
1. npm install electron
2. npm install electron-squirrel-startup
3. npm install keypress
4. npm install screenshot
5. npm install screenshot-desktop
6. npm install uuid

Python Install Requirements
>>Open CMD and write command:- pip install -r requirements.txt

The whole project is taken screenshots of desktop and saved in temporary directory and capture golbal keypress and saved in root directory with timezone

